const registerUser = (req, res) => {
  res.json({ message: "User register (Stub)" });
};

const loginUser = (req, res) => {
  res.json({ message: "User login (Stub)" });
};

module.exports = {
  registerUser,
  loginUser,
};
